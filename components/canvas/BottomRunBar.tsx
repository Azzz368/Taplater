"use client";
import { Button } from "@/components/ui/Button";
import { useCanvasStore } from "@/store/canvasStore";
export function BottomRunBar() { const { selectedNodeId, runNode, runWorkflow, saveCanvas, loadCanvas, clearCanvas, lastError } = useCanvasStore(); return <footer className="flex min-h-14 items-center gap-2 border-t border-slate-800 bg-[#0c1622] px-4"><Button disabled={!selectedNodeId} onClick={() => selectedNodeId && void runNode(selectedNodeId).catch(() => undefined)}>Run selected</Button><Button className="border-cyan-500/50 bg-cyan-400/10 text-cyan-100" onClick={() => void runWorkflow()}>Run workflow</Button><div className="mx-2 h-6 w-px bg-slate-800"/><Button onClick={saveCanvas}>Save</Button><Button onClick={loadCanvas}>Load</Button><Button onClick={clearCanvas}>Clear</Button>{lastError && <p className="ml-2 text-xs text-rose-300">{lastError}</p>}<p className="ml-auto text-[11px] text-slate-500">Mock AI provider · local-first canvas</p></footer>; }
